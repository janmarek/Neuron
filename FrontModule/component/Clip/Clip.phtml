<p n:if="$user->isAllowed('clip', 'edit')">
	<a href="{plink :Admin:Clip:edit $clip->getId()}" target="_blank">Upravit</a>
</p>

{cache $clip->cacheKey, tags => array($clip->cacheKey)}
{!$clip->text|texy}
{/cache}